// pictureData.js
import React, { useState } from "react";

export const  = [];


const PictureListApp = () => {
  const [pictures, setPictures] = useState(PictureList);
  const [newPictureUrl, setNewPictureUrl] = useState("");

  return (
    <div>
  
    </div>
  );
};

export default PictureListApp;
